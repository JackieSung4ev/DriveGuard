# DriveGuard Web

Vue 3 + Vite frontend for the DriveGuard Web UI.

```bash
npm install
npm run dev
```

The development server proxies `/api` to `http://127.0.0.1:8080`. If the API is not running, the frontend uses local mock data in development.

```bash
npm run build
```
